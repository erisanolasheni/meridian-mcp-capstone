# Copy to `.env` for local runs. Do not commit `.env` (see `.gitignore`).
# Hugging Face GitHub deploy uses repo Secrets — see README.

OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
# LLM_BASE_URL=https://openrouter.ai/api/v1

MCP_URL=https://order-mcp-74afyau24q-uc.a.run.app/mcp
